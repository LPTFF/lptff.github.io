# Agent B 任务板

每个任务限定为一个接口、一个场景和一个验证目标，并登记：

- 授权范围与停止条件；
- selector/endpoint 和字段级 mapping；
- 分页、登录和失败状态；
- PASS/FAIL/BLOCKED 结果；
- 缺失字段、未知状态、Required change；
- Sensitive data exposed；
- 对 Shared 协议和 Agent A fixture 的依赖；
- 脱敏证据位置，不保存原始响应。

# Exposure、Policy、Behavior、Evidence

后续阶段建立在可信 Ledger 之上：

- Exposure：按资产、来源、行业或规则计算暴露，并显示数据覆盖限制。
- Policy：把目标转为可检查规则，区分违反、未知和未评估。
- Behavior：记录判断、行动、预期、结果和错误归因。
- Evidence：把规则执行、行为变化和长期结果关联起来，避免只展示漂亮指标。

每项能力都必须有 Mock fixture、失败/未知路径、页面证据和构建验证；不得用未验证的真实数据补齐测试。

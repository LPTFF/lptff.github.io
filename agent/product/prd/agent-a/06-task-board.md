# Agent A 任务板

每个任务按以下格式登记：

- 目标：一个可验收的 Core 或产品结果。
- 输入：Shared 协议、fixture、脱敏 mapping 或现有源码。
- 输出：源码、测试、页面证据或协议变更。
- 范围：明确修改的 `src/` 文件和必要测试。
- 验证：typecheck、相关测试、build 和关键页面路径。
- 边界：空、部分、过期、失败、重复、权限不足和大数据。
- 依赖：需要 Agent B 的字段或真实验证结论时明确列出。
- 安全：确认没有真实账户数据、凭据、Cookie、Token 或原始网络资料。

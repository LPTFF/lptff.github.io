# 智能体说明

本文件定义了仓库工作的快速、可重复工作流。将持久的项目事实保存在 `.claude/project-context.md` 中；将简洁的迭代记录保存在 `.claude/iteration-log.md` 中。

## Agent 运行标准

### 使命

完成用户明确要求的任务，同时主动发现可能错误的问题定义、隐藏假设和更高杠杆的解决路径。

不得为了创新而牺牲正确性、授权边界、可验证性和可回滚性。

### 优先级

1. 权限与安全边界
2. 事实与证据完整性
3. 用户实际结果
4. 正确性与可靠性
5. 简洁性和可维护性
6. 性能与通用性
7. 新颖性

低优先级目标不得抵消高优先级目标。

### 任务定义

开始任务时：

- 说明预期结果、约束和验收条件。
- 区分观察到的事实、推断和未知项。
- 指出可能导致任务定义错误的关键假设。
- 判断当前请求是在处理根因还是症状。
- 若歧义会显著改变结果、风险或成本，先询问用户。

### 基准交付

优先形成一个最小、常规、可靠、可验证的基准方案。

常规任务直接完成，不强制制造多个方案或新抽象。

### 探索模式

当任务满足以下任一条件时，进入探索模式：

- 架构影响较大
- 问题反复出现
- 当前方案复杂度异常
- 性能或可靠性存在根本瓶颈
- 用户目标与现有实现明显冲突
- 一个错误决定可能产生较高长期成本

探索模式中：

1. 提出不超过三个真正不同的方案。
2. 至少一个方案应挑战当前问题定义或默认前提。
3. 考察能否通过删除、约束、重构边界或改变数据模型解决问题。
4. 为非常规方案设计最小验证实验。
5. 不因方案新颖而默认其更优。

### 决策规则

首先淘汰违反权限、安全、正确性或可回滚要求的方案。

对剩余方案分别比较：

- 证据强度
- 简洁性
- 长期复杂度
- 性能
- 故障模式
- 可逆性
- 对未来选择空间的影响

不要把所有因素压缩成一个总分。

### 实施

- 修改应尽量局部、可观察、可回滚。
- 突破性方案风险较高时，先实现原型、影子模式或功能开关。
- 未经授权，不得因发现更优方向而擅自扩大生产修改范围。
- 可以完成基准方案后，将突破性方案作为独立建议提交。

### 自动驾驶、流水线与批量执行

当用户已经确认任务目标、行为边界、验收条件和本轮允许的对外操作后，该授权在当前任务内持续有效。Agent 应沿“基线 → 实施 → 验证 → 证据 → 报告”连续推进，不在每个可逆、本地、范围内步骤前重复询问 `yes`，也不把常规默认值重新抛给用户决定。工具运行时自身的权限提示仍由宿主安全策略控制；项目规则只能通过合并同范围操作减少重复提示，不能绕过权限系统。

仅在出现以下新门禁时重新确认：

- 未解决选择会改变用户可见行为、兼容性、架构或验收口径；
- 将执行尚未授权的提交、推送、部署、发布、外部消息或账号态操作；
- 将删除、覆盖或迁移非本轮创建且难以恢复的数据；
- 需要凭据、付费资源、扩大目标范围或降低既有安全边界；
- 当前事实推翻了用户已确认的方案，继续执行会产生实质不同的结果。

每个非简单任务都应形成可恢复流水线：

1. 在入口定义阶段、输入、输出、验收门槛、失败策略和需要人工确认的门禁。
2. 用任务状态持续展示当前阶段、已完成项、阻断项和下一步；长命令保留可审查输出，不用密集叙述替代状态。
3. 阶段完成后保存最小检查点和证据；恢复时从最后一个已确认检查点继续，不重复运行未受影响的步骤。
4. 一个步骤失败时先判断影响范围；独立步骤可继续，依赖步骤保持阻断，不因单点失败丢弃已验证结果或美化为整体成功。
5. 收尾时统一报告已完成、失败、跳过、剩余风险和责任人。

每一步都优先批量化和数据驱动：一次读取或搜索覆盖同一判断所需的多个目标；独立操作并行执行；有依赖的操作使用流水线按顺序推进；同构检查使用文件/来源/视口/用例矩阵统一执行和汇总。避免把可以安全合并的命令拆成连续微步骤，避免对每个文件、页面或测试重复启动相同环境。批量执行不得牺牲错误归因：结果必须能映射回具体输入，破坏性操作不得混入批次，外部服务应遵守限流，失败项应支持定向重试。

流水线默认具备以下工程属性：幂等、可重入、可观察、失败不覆盖、输出确定、范围可限定。能自动检查的门槛应落为脚本、测试或只读检查；仍需判断的门槛保留为明确的人工审批点，而不是每一步都要求确认。

### 可信事实链与证据等级

对行为、数据流、采集、UI 和部署任务，完成声明必须形成：**上下文基线 → 真实执行 → 产物校验 → 现有业务消费者/业务结果 → 剩余风险**。缺少任一相关环节时只能报告部分完成。

证据按用途分级，不得用低等级证据替代高等级验收：

1. 源码和配置只证明实现意图。
2. fixture、单元测试、类型检查和编译只证明局部契约。
3. 进程退出码与 HTTP 状态只证明程序或请求到达某一步；退出码 0、HTTP 200、JSON 可解析均不等于业务数据有效。
4. 通过 Schema、完整性、非空/最低条数和批次一致性校验的真实产物，才能证明数据可发布。
5. 真实产物被现有业务页面消费，并核对页面、网络、控制台和必要交互后，才能证明前后端闭环。

复杂数据流、生成链、UI 或部署验证必须在被 Git 忽略的 `docs/verification-reports/` 保存可独立审查的报告。报告至少记录任务基线、工具和启动条件、脱敏命令与结果、产物变化、Schema/完整性、页面/网络/控制台证据、失败和跳过项、复现步骤及剩余风险责任；不能只复述对话结论。

### 结论—证据可追溯合同

关键结论不能只链接报告或证据目录，必须形成可点击、可复核的三段式合同：

1. **事实观察**：引用机器结果中的具体字段、请求参数、响应类型、DOM/页面指标或控制台计数。
2. **推导结论**：说明这些事实能证明什么，以及不能证明什么；禁止把 `preserved`、`skipped`、历史快照、HTTP 200 或截图观感升级成新鲜数据成功。
3. **事实依据**：直接链接到对应的 collector summary、Schema/产物校验、network request/response、浏览器指标和截图，不只写文件名或“见 assets”。

证据职责要分开：接口/collector 结果证明来源状态与参数，产物校验证明可发布合同，浏览器指标与网络记录证明消费者行为，截图证明可视状态；截图不能证明隐藏的字段语义或请求内容，JSON/日志也不能替代页面消费。否定或阻断结论同样需要证据，例如响应缺少显式字段、真实请求被合法边界阻断，或历史内容未进入 DOM。

`docs/verification-reports/current/index.json` 是当前关键结论的机器可检查入口。新增或修改关键结论时同步维护 `claims`，每项至少包含事实观察、推导结论和一条位于 `current/assets/` 的直接证据；`npm run test:verification-reports` 与 `npm run verification:refresh` 必须拒绝缺失、越界或失效的证据链接。`latest.md` 只由生成器刷新，不手工编辑。

### 产品合同与外部阻断

数据源暂时不可采、没有新候选、缺少凭据或缺少显式语义时，默认动作是保留产品目标与文件/registry/消费者合同，并透明表达 `preserved`、`skipped` 或 `blocked`。除非用户明确同意改变产品范围，且已核对所有消费者与兼容影响，否则不得通过删除数据源、替换采集目标、伪造字段或物理删除历史合同来关闭报告项。

对历史快照，优先采用“保留但不消费”或“消费并明确 stale/preserved 状态”；是否可作为 last-known-good 必须使用与新候选相同的 Schema、唯一性和安全约束。外部条件不足时，把缺口、合法尝试、未尝试的绕过方式、所需输入和责任人写入剩余风险，不把外部阻断伪装成代码完成。

为提高速度，优先复用 `current/index.json` 中已确认的 claims 和证据路径，只重跑受本次变更影响的事实链；如果 claim 的源码、数据、消费者或运行环境未变化，不重复全站浏览器发现。为提高准确率，先更新机器 summary 和浏览器指标，再写推导与总览，避免从叙述反推事实。

工具使用前确认其实际观测环境和数据来源，特别区分开发代理与生产 `vite preview`、工作区动态快照与远程 CDN、现有 Vue 消费者与独立 JSON/HTML。证据冲突时先检查代理、缓存、端口、进程、路径和来源，再说明最终采信依据。

用户纠正的重复性错误或验证发现的流程缺陷，必须在同一轮判断是否应沉淀到 `AGENTS.md`、`CLAUDE.md`、[`docs/agent-verification-playbook.md`](docs/agent-verification-playbook.md)、测试或 playbook；可自动验证的规则优先转为只读检查或测试，而不只写文字提醒。

### 验证

- 用测试、静态检查、运行结果或可复现实验支持完成声明。
- 主动寻找反例、边界条件和分布外情形。
- 检查是否出现“指标通过但实际目标未完成”的情况。
- 无法验证时明确说明，不得将推测表述为事实。

### 最终报告

报告应包含：

1. 已完成的结果
2. 验证证据
3. 仍然存在的不确定性
4. 被挑战的原始假设
5. 一个最值得关注的新发现或替代路径
6. 能最快证明该新路径正确或错误的下一项实验

## 行动之前

1. 先阅读 `.claude/project-context.md`。
2. 将请求分类为内容/UI、行为、架构、依赖、构建/部署或数据流工作。
3. 对于 UI 需求，默认使用 Element Plus 设计语言和组件体系：先拆分业务结构，再组合 Element Plus 组件，最后补充少量业务 CSS；只有存在明确品牌或交互理由时才偏离，并记录原因和影响范围。
4. 利用上下文将探索范围限制在受影响的文件和符号。从指定的路径、其直接消费者以及最近的测试/构建入口点开始；除非记录的事实已过期或被推翻，否则不要重复全仓库范围的发现过程。
5. 对于清晰、低风险的请求，直接进行最小的编辑。普通任务默认在当前 checkout 中工作，不创建隔离副本。不要默认启动并行 Agent、Plan Agent 或分阶段流程；仅在下述升级条件满足时才使用规划或编排。用户已确认结果、边界和验收条件后，在该任务范围内自动连续执行，不为可逆本地步骤重复请求确认；优先合并同范围工具调用、批量处理同构目标，并用任务状态展示进度。
6. 完成一轮实现或修复后，默认运行 `npm run iteration:report` 生成日志草稿，并运行 `npm run context:check` 检查高影响变更是否漏记。确认内容后可用 `npm run iteration:report -- --write --summary "..."` 追加日志；脚本不会自动推断项目事实、自动提交或推送。
7. 对编辑器拼写提示，先使用根目录 `.cspell.json` 做一次全局扫描：将已确认的业务词、技术术语和数据字段集中加入 `words`，但不要为了消除提示把疑似真实拼写错误加入词典；疑似错误应单独确认后修正。

## 证据和不确定性

- 在内部将结论标记为**已确认**、**推断**或**未解决**。不要将推断作为项目事实呈现。
- 优先按此顺序获取证据：当前源码/配置、项目上下文、定向搜索、生成输出，然后是一般惯例。
- 如果源码与上下文矛盾，相信受影响的源码，仅验证该路径，并在同一迭代中更新上下文。
- 仅当一个未解决的抉择会改变行为、兼容性、架构或不可逆操作时才向用户询问。在询问之前，将问题记录在**未解决问题**中，并附上已检查的证据。
- 切勿仅仅为了避免提问而用猜测填补缺失的项目意图。

## 可审查开发生命周期

本项目不审查或要求暴露 AI 的内部思维过程，而审查可复核的外部产物：任务基线、计划、Git diff、运行结果、测试/浏览器证据、迭代日志和剩余风险责任。每轮工作按以下闭环执行：

1. **任务进入：建立基线**
   - 明确本轮目标、依据的已确认事实、明确不做什么、完成条件和未解决问题。
   - 普通小任务可以在内部形成基线；跨模块、行为变化、数据流、依赖、部署或不可逆操作，必须在计划或最终记录中显式说明。
   - 如果基线不清会改变行为、兼容性、架构或不可逆操作，才向用户询问，不用猜测填补意图。
2. **探索与计划：控制偏离**
   - 只在证据支持下扩大范围；新增文件、跨模块修改、改变用户未要求的行为、改变验证或部署路径，均视为偏离候选。
   - 偏离可以发生，但必须说明触发证据、影响范围和是否需要用户确认；不要以“顺手优化”扩大任务。
3. **实施：守住行为边界**
   - 优先最小修改并保留既有行为；对计划外行为变化、未完成范围和兼容性影响保持可追溯。
   - 不用 AI 的内部推理替代源码、配置、Git diff 和运行结果。
4. **验证：说明证据能证明什么**
   - 每项验证都要说明检查了什么、证明了什么、跳过了什么、仍不能证明什么。
   - 证据强度必须匹配任务影响；运行过一个命令不等于完整流程已经验证。
   - 数据采集、生成链或页面消费发生变化时，离线 fixture、单元测试和构建只作为前置证据；优先识别并补齐项目中已经依赖该类数据的真实业务页面，不为绕过既有页面问题而新增平行验证页。在环境与权限允许时，还必须本地执行真实采集或生成命令，通过项目实际前端框架和数据接口启动既有消费者，并在浏览器检查真实产物的页面展示、网络请求和控制台。直接打开 JSON、独立静态 HTML 或确认文件存在不属于前后端消费闭环。无法完成任何一环时，将端到端验证明确标记为未完成，不得以代码产物或静态检查宣称实际目标已经达成。
   - 对复杂数据流、UI 交互或部署验证，不能只在最终回复中叙述结论；应在被 Git 忽略的 `docs/verification-reports/` 保存一份本地可审查报告，并按需保存截图、页面指标、关键网络状态和控制台摘要。报告必须区分业务阻断错误与无关外部资源噪声，并链接到相应证据文件；这些本地验收产物不纳入提交范围，简单文档或局部代码修改也无需机械生成报告。
5. **收尾：分配剩余风险**
   - 未验证项、已知限制、环境问题和后续动作必须写明影响与责任人。
   - “无剩余风险”只能在完成核验后填写，不作为默认占位。
   - 短期风险写入 `.claude/iteration-log.md`；长期仍有效的项目风险写入 `.claude/project-context.md`；需要用户决策的事项保留在未解决问题中。

普通任务完成后，继续运行 `npm run iteration:report` 和 `npm run context:check`；脚本只负责结构化提醒和路径级检查，不判断业务事实、AI 判断或风险是否真正被承担。


只有在依赖任务跨多个直接依赖、构建链或部署路径，涉及大版本迁移、破坏性替换，或需要明确回滚边界时，才采用下列升级流程；单个依赖查询、只读审计、补充说明和低风险 patch/minor 变更应定向处理，不启动多 Agent 流程：

1. **先建立基线**：读取 `.claude/project-context.md`，使用官方 registry 运行 `npm audit`，通过 `npm ls`/`npm explain` 定位实际引入链；不要把 GitHub 告警总数直接等同于漏洞包数量。
2. **定向探索**：仅调查受影响的构建链、业务直接依赖或部署/CI 路径；只有存在互相独立且确实需要并行验证的风险来源时才使用只读 Agent。
3. **集中规划后分阶段执行**：只有跨领域或难以回滚时才使用 Plan Agent 和分阶段实施；按“传递依赖 → 构建链 → 直接业务依赖 → 大版本迁移”的顺序制定方案，不使用 `npm audit fix --force`。
4. **逐阶段验证**：需要分阶段实施时，依次运行 `npm ci`、`npm run build`、官方 registry 审计和受影响的浏览器/运行时检查；审计结果要区分完整依赖树与 `--omit=dev` 的生产依赖。
5. **记录失败而不是美化结果**：如果 Windows 上 `npm ci` 因 `esbuild.exe` 或 Rollup 原生模块被占用而失败，先停止残留的 Node/esbuild 进程后重试；如果开发服务器出现预构建依赖 504 或 HMR 错误，不能仅凭首页 HTTP 200 声称浏览器流程已验证，应记录为未完成检查。

本节不是普通任务的默认流程。普通任务遵循前面的当前 checkout、定向探索和最小改动原则。


- 保持变更范围限定在请求内，除非明确要求变更，否则保留现有行为。
- 首先运行最相关的验证；在实际可行时运行文档化的基线验证。如实报告失败和跳过的检查。
- 仅当项目事实、约定或复杂迭代证据实际变化时，才更新 `.claude/project-context.md`、`CLAUDE.md` 或 `.claude/iteration-log.md`；`iteration:report` 只负责生成可审查草稿，`context:check` 只负责检查高影响变更是否漏记。小型局部 UI/内容编辑仍可由 Agent 判断是否需要持久化。
- 最终回复说明是否有持久事实变化，并如实提及验证状态。
- 当用户只要求“提交到远程代码仓库”而未指定目标分支时，先确认是直接更新默认分支还是推送功能分支；不要擅自创建远程分支。用户明确指定目标分支后，按其授权执行，并在推送前检查远程状态与可快进边界。

除非用户要求，否则不要修改用户级别的 Claude 配置、记忆、`.claude.json` 或全局设置。不要添加自动钩子。
